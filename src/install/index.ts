/**
 * Placeholder entrypoint for future install/uninstall workflow code.
 *
 * The PRD requires a dedicated CLI install path that deploys the editor plugin
 * into a Godot 4.6.x project. The scaffold creates the module boundary now so
 * later work can grow without reshaping the package.
 */
export function getInstallScaffoldNotice(): string {
  return "Install workflow scaffolded but not implemented yet.";
}
